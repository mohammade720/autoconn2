import 'package:flutter/material.dart';

class PartPurchase extends StatelessWidget {
  const PartPurchase({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello 2"),
    );
  }
}
